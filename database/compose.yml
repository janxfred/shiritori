services:
  postgres:
    image: postgres:16
    restart: always
    volumes:
      - db-store:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    env_file:
      - .env

  redis:
    image: redis:7
    restart: always
    ports:
      - "6379:6379"

volumes:
  db-store:
